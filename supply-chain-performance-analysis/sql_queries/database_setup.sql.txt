CREATE DATABASE SupplyChainDB;
USE SupplyChainDB;

CREATE TABLE SupplyChainSales (
    OrderNumber VARCHAR(20),
    SalesChannel VARCHAR(50),
    WarehouseCode VARCHAR(20),
    ProcuredDate DATE,
    OrderDate DATE,
    ShipDate DATE,
    DeliveryDate DATE,
    CurrencyCode VARCHAR(10),
    SalesTeamID INT,
    CustomerID INT,
    StoreID INT,
    ProductID INT,
    OrderQuantity INT,
    DiscountApplied DECIMAL(5,2),
    UnitCost DECIMAL(10,2),
    UnitPrice DECIMAL(10,2)
);

SHOW VARIABLES LIKE 'secure_file_priv';

LOAD DATA INFILE 'C:/ProgramData/MySQL/MySQL Server 8.0/Uploads/Supplychain_Data.csv'
INTO TABLE SupplyChainSales
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS;

SELECT COUNT(*) FROM SupplyChainSales;